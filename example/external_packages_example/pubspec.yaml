name: external_packages_example
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.2

dependencies:
  # path: ^1.8.0
  random_classes:
    path: ../../packages/random_classes
  tachyon:
    path: ../..

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
